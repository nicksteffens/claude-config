{{page-title "Movable Ink" separator=" - " prepend=true}}

<MainContainer @id="app-container" @class="flex flex-row min-h-screen bg-white">
  {{#unless this.iframeApi.runningInsideIframe}}
    <PersistentNavigation />
  {{/unless}}
  <div class="flex flex-col w-full bg-white min-w-0">
    {{outlet}}
  </div>
  <SnackBar />
</MainContainer>

{{#if (or this.currentUser.isOperator this.currentUser.isInternalEditor)}}
  <KeyboardShortcut @key="alt+f" @down={{set this "quickSearchVisible" true}} />

  {{#if this.quickSearchVisible}}
    <QuickSearch @onClose={{set this "quickSearchVisible" false}} />
  {{/if}}
{{/if}}

<ToolTipManager />

<ContextMenu @activeMenu={{this.contextMenus.activeMenu}} />
{{#if this.companySwitcher.error}}
  <SwitchCompanyModal />
{{/if}}